#version 450

layout(local_size_x = 256) in;

layout(binding = 0) buffer Data
{
    float values[];
};

void main()
{
    uint id = gl_GlobalInvocationID.x;

    if(id < values.length())
    {
        values[id] += 1.0;
    }
}
